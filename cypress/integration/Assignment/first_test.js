/*import Home from "../../pages/home"
/// <reference types="cypress" />

describe('login', function()
{
    const home = new Home()
    it ('sign_in', function (){
        home.open_page()
        home.title().should('eq','UI Testing Site')
        })
    it ('sign_in', function ()
    {
        cy.get('#if_logo').should('be.visible')
        //cy.get('#home').should('be.visible').click()
        cy.wait(2000)
        cy.get('ul').children('.active').children().should('have.id', 'form')

    })
})
*/